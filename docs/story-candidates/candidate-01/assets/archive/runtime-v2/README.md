# `runtime-v2` 历史素材归档

本目录保存《断冠之誓》旧像素风运行时包、三份旧 Manifest 与三份旧 QA 结果，仅用于追溯和差异对照。

- 当前游戏不得从此目录加载资源。
- 当前唯一正式素材包是 [`../../final-fantasy-v1/`](../../final-fantasy-v1/)。
- 旧目录结构原样保存在 [`runtime/`](runtime/) 中，未删除历史母图、预览或验收证据。
- 归档时间：2026-08-12。

如需比较两代素材，应使用各自 Manifest 的 `topicId`；不要把归档内的相对路径重新写回业务代码。
